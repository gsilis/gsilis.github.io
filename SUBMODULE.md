# Adding a new project

1. Open the `projects.json` file, and describe the new project.
  - Note: the `id` of the project needs to match the name of the folder that gets created as the submodule.
1. Add the project's repository as a submodule - `git submodule add <url>`
1. Run the install and build commands
1. Push the change